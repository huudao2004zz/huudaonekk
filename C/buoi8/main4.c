#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
int helo();
int main(int argc, char *argv[]) {
	int a,b;
	printf("nhap id: nhap pass: ");
	scanf("%d && %d",&a,&b);

	if (a==123 && b == 456);
	helo();
	return 0;
}
helo()
{
		printf("1:xem so du\n2:rut tien\n3:chuyen khoan\n4:ket thuc\n");
	return 0;
}
